{
  "name": "@openbot/plugin-browser-tools",
  "version": "1.0.0",
  "type": "module",
  "description": "Isolated browser tools for OpenBot",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --external:ai --external:zod --external:melony --external:@melony/ui-kit --external:@browserbasehq/stagehand"
  },
  "dependencies": {
    "@browserbasehq/stagehand": "^3.0.8"
  },
  "devDependencies": {
    "@melony/ui-kit": "^0.1.4",
    "esbuild": "^0.21.0",
    "melony": "^0.2.8",
    "zod": "^3.23.8"
  }
}
